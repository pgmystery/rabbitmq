[project]
name = "test-project"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "pika>=1.3.2",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
